# ngx-compress-rs v0.2.0 cycle

- Status: active
- Updated: 2026-08-06T10:26:42+08:00
- Workspace: `/Users/ryan/Documents/workspace.nosync/jiya·mira/ngx-compress-rs`
- Branch: `master`
- Base commit: `ca93d3fd597823dc4298732e9484c7d5f5a6a348`
- Current commit: `8483757980b0a2e0106552ce5ca5363cebdcb02b`

## Goal and Done Criteria

Implement the v0.2.0 Technical Preview cycle: ARM64 file-buffer correctness,
bounded resumable compression, strict negotiation with profile-aware priority,
compression statistics, ARM64 CI, documentation, and release metadata. Finish
with clean local commits and the strongest available local/integration/canary
evidence; do not push, publish, or replace the production module.

## Sub-goals

| Sub-goal | Status | Evidence | Next action |
|---|---|---|---|
| Production and ARM64 | editing | `codex/v020-production` worktree; ARM bitfield root cause reproduced and file gzip/br/zstd decode after C-shim fix | finish full integration gate and commit |
| Bounded streaming | editing | `codex/v020-budget` worktree; core budget/config tests pass | finish NGINX NULL-body-filter resume scheduling and commit |
| Strict negotiation | awaiting-feedback | `codex/v020-negotiation` worktree; core and module clippy pass | finish NGINX smoke and commit |
| Statistics | validated | commits `44d1b47`, `207c5e6`, `fe9ac74`; core tests and temporary NGINX module clippy pass | integrate after other runtime commits and add smoke coverage |
| v0.2.0 metadata | validated | commit `8483757` | update operator/release docs after feature merge |

## Confirmed Facts

- Production empty compressed files come from ARM ABI bitfield layout mismatch in generated `ngx_http_request_t` access, not from a codec-specific empty-stream bug.
- Server-Timing needs `expect_trailers` set before the downstream header filter and the trailer inserted before forwarding the last body buffer.
- `compress_stats off` avoids timing and byte-count collection; variables remain registered but resolve as not found without an active statistics context.

## Open Hypotheses

- The bounded-work continuation should follow NGINX filter buffering semantics: retain pending input, mark the connection buffered, and continue on a later body-filter call with null input. Real gzip decode must prove the final scheduling mechanism.

## Decisions

- Parallel work uses isolated worktrees and local Conventional Commits; the main worktree owns integration.
- `compress_static always` remains an explicitly warned negotiation bypass.
- Remote `oc.ams` work is limited to an isolated canary unless separately authorized.

## Validation

- `cargo test -p ngx-compress-core` — passed with statistics model tests.
- `cargo clippy -p ngx-compress-core --all-targets -- -D warnings` — passed.
- Temporary macOS NGINX source plus `cargo clippy -p ngx-compress-module --all-targets -- -D warnings` — passed for statistics integration.

## Working Tree and Ownership

- Main worktree is clean before this handoff file; it owns statistics and final integration.
- Three named worktrees own production/ARM64, bounded streaming, and strict negotiation respectively. Preserve their uncommitted changes until each agent reports a commit.

## Blockers, Risks, and Required Approval

- Full Docker gates require an available Docker/Colima daemon; it was not running at the first check.
- Production replacement, push, prerelease publication, and tag creation are not authorized.

## Next Actions

1. Cherry-pick completed workstream commits one at a time and resolve only explicit shared-interface conflicts.
2. Add statistics smoke coverage against the merged runtime.
3. Run cheap gates, then one full integration/security/canary batch.
4. Update canonical v0.2.0 documentation and mark this handoff complete.
